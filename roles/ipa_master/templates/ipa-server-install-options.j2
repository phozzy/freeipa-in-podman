--realm={{ realm }}
--allow-zone-overlap
--ds-password=
--admin-password=
{% for forwarder in forwarders %}
--forwarder={{ forwarder }}
{% endfor %}
--auto-reverse
--setup-dns
--unattended
--hostname={{ container_hostname }}.{{ domain }}
